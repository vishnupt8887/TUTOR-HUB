import { SclasspipePipe } from './sclasspipe.pipe';

describe('SclasspipePipe', () => {
  it('create an instance', () => {
    const pipe = new SclasspipePipe();
    expect(pipe).toBeTruthy();
  });
});
